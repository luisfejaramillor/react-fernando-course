// import { getHeroById } from "./getHeroById";

// export const getHeroByIdAsync = (id) => {
//     return(
//          new Promise((resolve, reject) => {
//             setTimeout(() => {
//                 const hero = getHeroById(id);
//                 hero ? resolve(hero) : reject(`Could not found hero with id: ${id}`)
//             }, 2000);
//         })
//     )
    
// }
